import React from 'react';
import Login from '@/app/components/login'; // Adjust the import path if necessary

const LoginPage = () => {
    return (
        <div>
            <h1>Login</h1>
            <Login />
        </div>
    );
};

export default LoginPage;